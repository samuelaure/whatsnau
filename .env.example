# --- Shared Infrastructure ---
NODE_ENV=development
LOG_LEVEL=info
PUBLIC_DOMAIN=whatsnau.localhost
DASHBOARD_URL=http://localhost:5173

# --- Shared Security ---
# Use a long random string (e.g., generated with 'openssl rand -base64 32')
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRES_IN=7d
# comma-separated list of allowed domains for CORS
ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com
INITIAL_ADMIN_PASSWORD=admin123

# --- Shared Database Credentials ---
DB_USER=db_user
DB_NAME=db_name
DB_PASSWORD=your_secure_db_password

# --- Deployment (Hetzner) ---
HETZNER_HOST=
HETZNER_USER=
HETZNER_SSH_KEY=
HETZNER_DEPLOY_PATH=whatsnau

# --- Shared Redis Configuration ---
REDIS_HOST=shared_redis
REDIS_PORT=6379
# REDIS_PASSWORD=

# --- Telegram (System-level - Required) ---
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_SYSTEM_CHAT_ID=your_admin_chat_id

# --- Meta App (Optional - for Embedded Signup) ---
# META_APP_ID=your_meta_app_id
# META_APP_SECRET=your_meta_app_secret

# --- WhatsApp Provider Selection ---
WHATSAPP_PROVIDER=meta
WHATSAPP_VERSION=v18.0

# --- Tenant Credentials (Optional - fallback only) ---
# These are optional and should be stored in the database per tenant
# Only use these for backward compatibility or initial setup

# WhatsApp (Meta Provider)
# WHATSAPP_PHONE_NUMBER_ID=
# WHATSAPP_PHONE_NUMBER=
# WHATSAPP_BUSINESS_ACCOUNT_ID=
# WHATSAPP_ACCESS_TOKEN=
# WHATSAPP_VERIFY_TOKEN=

# YCloud Provider
# YCLOUD_API_KEY=

# OpenAI
# OPENAI_API_KEY=
# PRIMARY_AI_MODEL=gpt-4o
# CHEAP_AI_MODEL=gpt-4o-mini

# Telegram (Deprecated - use database)
# TELEGRAM_CHAT_ID=
